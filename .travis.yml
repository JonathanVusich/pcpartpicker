language: python
python: 3.7
dist: xenial
sudo: true
matrix:
  include:
    - os: linux
      dist: xenial
      python: 3.7
      sudo: true
      before_install:
        - pip install coveralls
        - python setup.py install
    - os: windows
      language: sh
      python: "3.7"
      before_install:
        - choco install python3
        - export PATH="/c/Python37:/c/Python37/Scripts:$PATH"
        - python -m pip install coveralls
        - python setup.py install
script:
    - coverage run --source=pcpartpicker -m unittest discover
after_success:
    - coveralls
notifications:
  email:
    recipients:
      - jonathanvusich@gmail.com
    on_success: never
    on_failure: always